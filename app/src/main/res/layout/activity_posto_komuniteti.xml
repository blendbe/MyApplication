<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PostoKomuniteti">
     <ImageView
         android:layout_marginTop="10dp"
         app:srcCompat="@drawable/post_placeholder"
         android:id="@+id/imazhiPosto"
         android:layout_width="match_parent"
         android:layout_height="190dp"/>
    <EditText
        android:id="@+id/tekstiPosto"
        android:background="@drawable/rounded"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/imazhiPosto"
        android:layout_width="match_parent"
        android:layout_height="200dp" />
    <Button
        android:textAllCaps="false"
        android:textColor="@color/whiteColor"
        android:text="Posto"
        android:background="#0085ff"
        android:layout_marginTop="10dp"
        android:id="@+id/btnPosto"
        android:layout_below="@+id/tekstiPosto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ProgressBar
        android:id="@+id/progressBar2"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:visibility="invisible" />

</RelativeLayout>